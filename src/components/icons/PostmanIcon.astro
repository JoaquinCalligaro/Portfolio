---
import { Image } from 'astro:assets';
import postmanSvg from '../../assets/svg/postman.svg';

export interface Props {
  class?: string;
  title?: string;
  wrapperClass?: string;
  glow?: boolean;
}
const {
  class: cls = 'h-6 w-6',
  title = 'Postman',
  wrapperClass = null,
  glow = true,
} = Astro.props;
import '../../styles/projects.css';
const base =
  'inline-flex items-center justify-center p-1 rounded-md transition-colors duration-200 mr-2';
const hover = 'hover:bg-orange-50 dark:hover:bg-orange-900';
const wrapper =
  wrapperClass ?? `${base} ${hover} ${glow ? 'glow-effect' : ''} icon-postman`;
---

<span class={wrapper} aria-hidden="true">
  <Image src={postmanSvg} class={cls} alt={title} loading="lazy" />
</span>
