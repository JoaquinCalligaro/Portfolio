---
export interface Props {
  class?: string;
  title?: string;
}
const { class: cls = 'h-6 w-6', title = 'HTML' } = Astro.props;
---

<!-- HtmlIcon: usa el SVG estático desde public/svg y permite clases Tailwind -->
<img src="/svg/html.svg" class={cls} alt={title} loading="lazy" />
