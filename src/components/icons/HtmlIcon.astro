---
export interface Props {
  class?: string;
  title?: string;
  wrapperClass?: string;
  glow?: boolean;
}
const {
  class: cls = 'h-6 w-6',
  title = 'HTML',
  wrapperClass = null,
  glow = true,
} = Astro.props;
import '../../styles/icon-effects.css';
const base =
  'inline-flex items-center justify-center p-1 rounded-md transition-colors duration-200 mr-2';
const hover = 'hover:bg-orange-50 dark:hover:bg-orange-900';
const wrapper =
  wrapperClass ?? `${base} ${hover} ${glow ? 'glow-effect' : ''} icon-html`;
---

<!-- HtmlIcon: usa el SVG estático desde public/svg y permite clases Tailwind -->
<span class={wrapper} aria-hidden="true">
  <img src="/svg/html.svg" class={cls} alt={title} loading="lazy" />
</span>
