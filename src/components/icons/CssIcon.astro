---
export interface Props {
  class?: string;
  title?: string;
}
const { class: cls = 'h-6 w-6', title = 'CSS' } = Astro.props;
---

<!-- CssIcon: usa el SVG estático desde public/svg y permite clases Tailwind -->
<img src="/svg/CSS.svg" class={cls} alt={title} loading="lazy" />
